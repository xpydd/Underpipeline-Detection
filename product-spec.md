# 需求文档

<!-- 需求文档元数据：开始 -->
- 项目名称：管网智慧探测系统
- 文档版本：v0.8.6
- 更新日期：2026-06-09
- 所属迭代：迭代八
- 文档状态：草稿
<!-- 需求文档元数据：结束 -->

## 1. 需求背景

当前项目是一个面向地下管线探测业务的管理与作业协同前端原型，已形成登录、项目管理、业主管理、系统级控制台、知识中心、系统设置以及项目工作台等页面。现阶段系统已经具备较完整的信息架构和交互雏形，但需求边界、角色职责、业务规则、模块间关系仍分散在页面原型和脚本中，缺少统一的中文需求基线文档。

本次需求文档的目标，是基于当前代码仓库中的现有原型页面，整理出一版可追溯、可持续维护的产品需求基线，用于后续迭代设计、开发、联调、验收和变更记录。

## 2. 目标与范围

### 2.1 目标

- 建立一份基于现有原型的系统级需求基线文档。
- 明确系统一级模块、二级工作台模块、角色分工和核心业务场景。
- 提炼当前原型中已经出现的业务状态、筛选维度、字段结构和交互模式。
- 为后续版本迭代提供统一的变更基线与验收依据。

### 2.2 范围

本次纳入范围的内容如下：

- 登录页面：账号密码登录、密码可见切换、基础表单校验、登录后跳转。
- 项目管理：项目列表、统计卡片、筛选、分页、新建/编辑/删除、项目详情入口。
- 项目工作台：
  - 项目详情
  - 控制台
  - 项目勘探
  - 项目数据
  - 项目模型
  - 项目报告
- 业主管理：业主列表、业主详情入口、合作项目数、启停状态、新增/编辑。
- 系统级设备控制台：作为一级导航入口，位于业主管理之后、知识中心之前，提供设备接入、连接、绑定、换绑、解绑、移除、绑定记录和设备回传记录等设备管理能力。
- 知识中心：作为独立一级模块存在，当前原型处于建设中状态。
- 系统设置：字典管理、用户管理、组织架构管理、角色管理。
- 全局能力：导航状态同步、路径解析、深色模式、本地示例数据、LocalStorage 临时存储。
- 雷达设备接入与项目级控制台：设备连接状态、作业模式选择、初始化参数下发、模拟适配层接入、MQTT 控制指令、设备状态回传、设备实时视频流入口、设备接入后自动传输数据、检测结果 `result_json` 原型导入解析、候选成果准确性研判、勘探成果接入、示例地图/模型自动联动与接入日志留痕。

### 2.3 非范围

本次明确不纳入以下内容：

- 后端接口定义与数据库物理表结构。
- 真正可用的登录鉴权、权限校验和会话安全机制。
- 地图服务、逆地理编码、真实设备联调、真实长连接运行环境、真实 GIS 地图服务、真实 3D 模型服务、正式后端入库接口。
- 知识中心的正式内容生产与发布流程。
- 3D 模型、报告生成、勘探执行细节的完整业务规则。

## 3. 用户角色与业务场景

### 3.1 角色定义

- 系统管理员：负责系统配置、用户、组织、角色、系统级设备控制台、设备连接状态、设备与项目绑定关系等后台能力维护。
- 项目经理：负责项目建立、计划安排、角色分配、进度跟踪和项目统筹。
- 勘探人员：负责项目勘探作业、现场问题处理、坐标录入等操作。
- 数据人员：负责项目数据导入、清洗、校核、汇总与展示。
- 报告人员：负责项目成果报告整理与输出。
- 审核人员：负责项目成果审核与质量把关。
- 业主联系人：作为外部协同对象存在，关注项目归属、联系人、合作项目关系。

### 3.2 核心业务场景

- 场景 1：管理员或项目经理登录系统后，查看项目总数、状态分布和待处理事务。
- 场景 2：项目经理在项目管理页创建项目，选择场景类型、业主单位、日期范围、联系人和项目成员。
- 场景 3：用户从项目列表进入单个项目，在项目工作台中查看项目详情、勘探、数据、模型和报告模块。
- 场景 4：数据人员在项目数据页进行数据查看、筛选、分页、汇总和导入校核。
- 场景 5：管理员维护业主资料、系统字典、用户、组织架构和角色。
- 场景 6：用户通过知识中心查阅技术文档、操作指南、FAQ、视频教程等资料。
- 场景 7：勘探人员在项目勘探控制台通过设备适配层连接雷达小车，下发本次测线初始化参数；设备接入成功后系统自动传输并载入检测数据，用户在数据接入前研判准确性，确认后将数据加入勘探成果。
- 场景 8：雷达数据进入勘探成果后，系统自动生成示例地图定位点和模型定位记录；后续项目数据阶段从勘探成果导入数据，不在雷达控制台直接写正式项目数据台账。
- 场景 9：项目负责人或勘探人员进入项目详情控制台，若项目已绑定设备，则先在主页面完成起点、方向、探测范围等高频规划字段，结合探索、遍历或复检作业策略，通过 MQTT 协议下发探测规划、继续、暂停、结束或遥杆控制指令，并查看设备电量、位置、实时视频、任务状态和探测结果回传；若项目未绑定设备，则提示联系管理员绑定设备。
- 场景 10：系统管理员进入系统级设备控制台，查看全部设备的在线状态、连接状态、当前绑定项目、历史绑定记录和设备回传记录；管理员可新增设备接入、移除未绑定设备、连接设备、将空闲设备绑定到项目、将已绑定设备换绑到其他项目，或在作业结束/设备回收时解除设备绑定，并保留操作留痕。

## 4. 功能需求

### 4.1 功能清单

- FR-01 登录与进入系统
- FR-02 一级导航与页面切换
- FR-03 项目管理
- FR-04 项目详情与项目级导航
- FR-05 项目勘探
- FR-06 项目数据管理
- FR-07 项目模型
- FR-08 项目报告
- FR-09 业主管理
- FR-10 知识中心
- FR-11 系统设置
- FR-12 全局配置与基础能力
- FR-13 雷达扫描数据接入
- FR-14 项目级设备控制台
- FR-15 系统级设备控制台

### 4.2 详细需求

#### FR-01 登录与进入系统

- 系统应提供登录页面，包含手机号和密码输入框。
- 系统应支持手机号格式校验与密码非空校验。
- 系统应支持密码明文/密文切换。
- 登录成功后，系统应跳转到项目管理页。
- 当用户点击“忘记密码”时，系统应给出联系管理员的引导信息。

#### FR-02 一级导航与页面切换

- 顶部一级导航至少包括：项目管理、业主管理、知识中心、系统设置。
- 系统应根据当前页面路径或 hash 自动识别激活菜单。
- 当用户点击同页菜单时，系统应支持当前内容区域切换并同步 hash。
- 系统应具备路径解析能力，以适配本地与 OSS 子目录部署。

#### FR-03 项目管理

- 系统应展示项目统计卡片，至少包括项目数和事务数。
- 系统应提供项目列表，字段至少包括：
  - 序号
  - 项目编号
  - 项目名称
  - 场景类型
  - 业主单位
  - 创建日期
  - 计划开始日期
  - 计划截止日期
  - 状态
  - 操作
- 系统应支持按项目名称/编号模糊搜索。
- 系统应支持按场景类型、状态、创建日期范围筛选项目。
- 系统应支持新建项目、编辑项目、删除项目。
- 系统应支持为项目维护以下信息：
  - 基础信息：编号、名称、地址、场景、描述
  - 时间信息：创建日期、计划开始日期、计划截止日期
  - 坐标与高程系统
  - 业主及联系人信息
  - 项目参与人员：项目经理、勘探、数据、报告、审核
- 系统应支持地图选点辅助填写项目地址；在无真实地图服务时可退化为模拟模式。
- 系统应支持从项目列表进入项目详情页。

#### FR-04 项目详情与项目级导航

- 项目详情相关页面应能从 URL 参数中识别当前项目 ID 或名称。
- 系统应在项目级头部展示项目名称、项目编号、项目地址。
- 项目级头部应采用紧凑单行布局，将项目名称、项目编号、地址和项目页签合并展示，减少头部占用，保证项目设备控制台等操作区在首屏获得更多可用空间。
- 项目级工作台应提供侧边导航，至少包括：
  - 项目
  - 控制台
  - 勘探
  - 数据
  - 模型
  - 报告
- 项目级页面跳转时应保留项目 ID 参数。

#### FR-05 项目勘探

- 系统应存在独立的项目勘探页面。
- 系统应支持与勘探作业相关的业务能力入口，包括但不限于：
  - 点查询
  - 雷达扫描
  - 勘测问题处理
  - 坐标录入
- 系统应保留作业确认、暂停、继续、导入导出等操作扩展点。
- 系统应在雷达扫描入口中提供设备接入原型能力，用于承接自动巡检雷达小车的初始化参数下发与检测结果接收。
- 项目级设备控制已以 MQTT 对接协议作为需求基线；后端接口、坐标转换、地图联动和正式成果入库规则在当前原型中尚未完全收敛，后续需补全。
- 勘探页面的扫描数据弹窗应承接设备返回结果列表、字段补录、分页勾选和导入勘探成果，不应与项目数据页的通用数据导入弹窗混淆。

#### FR-06 项目数据管理

- 系统应提供项目数据页，用于查看和处理项目数据。
- 数据页应至少包含数据操作菜单、数据视图区、分页或汇总相关能力。
- 系统应支持按管线类型进行筛选，当前原型中可识别的类型包括：
  - 全部
  - 给水
  - 燃气
  - 排水
  - 电力
  - 热力
  - 通信
- 系统应支持数据明细表格展示。
- 系统应支持分页浏览。
- 系统应支持切换到数据汇总视图。
- 系统应支持导入数据并在导入弹窗中查看“更新数据”和“新增数据”。
- 导入弹窗应支持单元格编辑、字段高亮提示、勾选是否采用。
- 数据字段至少覆盖点号、连线点号、长度、附属物、材质、管径、压力、流向、孔数、坐标、埋深、埋设方式、年份、权属单位等。

#### FR-07 项目模型

- 系统应提供独立的项目模型页面入口。
- 当前阶段将模型能力定义为项目工作台中的独立模块，用于承接后续三维展示、模型浏览和成果联动。
- 详细展示方式、模型来源与交互规则待补充。

#### FR-08 项目报告

- 系统应提供独立的项目报告页面入口。
- 报告模块用于承接成果汇编、导出、审核与展示相关能力。
- 当前原型已体现模块存在，但详细表单、报告模板和导出流程待补充。

#### FR-09 业主管理

- 系统应提供业主信息管理列表。
- 列表字段至少包括：
  - 序号
  - 业主编号
  - 业主名称
  - 法人代表
  - 创建日期
  - 创建人
  - 合作项目数
  - 状态
  - 操作
- 系统应支持按业主名称模糊搜索。
- 系统应支持按状态、创建日期范围筛选。
- 系统应支持合作项目数排序。
- 系统应支持新增、编辑、删除业主信息。
- 系统应支持维护业主基础信息，包括统一社会信用代码、所属地区、地址、单位性质、状态等。
- 系统应支持从业主列表跳转到业主详情页。

#### FR-10 知识中心

- 系统应提供独立的知识中心入口。
- 当前原型中知识中心处于“开发中”状态，但已明确后续内容方向，包括：
  - 技术文档
  - 操作指南
  - 常见问题
  - 视频教程
  - 行业标准
  - 案例库
- 当前阶段需保留模块入口和建设状态提示。

#### FR-11 系统设置

- 系统设置下应至少包含以下子模块：
  - 字典管理
  - 用户管理
  - 组织架构管理
  - 角色管理
- 这些模块已在一级导航下拉菜单中出现，应视为系统后台配置范围。
- 系统级设备控制台不再作为系统设置下的二级菜单描述，统一作为一级“控制台”入口维护，详细规则见 FR-15。
- 当前版本主要明确系统设置入口与信息架构，系统设置各子模块的详细字段与操作规则待补全。

#### FR-12 全局配置与基础能力

- 系统应支持深色模式切换，并使用 LocalStorage 保存状态。
- 系统应支持本地示例数据或 LocalStorage 数据混合展示，以便前端原型联调。
- 系统应支持通过统一配置处理部署路径问题，包括但不限于：
  - 自动检测项目部署的基础路径（Base URL），适配根目录部署与子目录部署场景。
  - 当项目部署在 OSS Bucket 子目录（如 `/Underpipeline-Detection/`）时，确保所有页面跳转、导航链接和资源引用均使用正确的完整路径。
  - 当使用相对路径时应保证正确解析，当使用绝对路径时应能自动补全项目前缀。
  - 提供统一的路径解析 API（如 `APP_CONFIG.resolvePath`）供各模块调用。
- 系统应保证导航链接、项目参数和局部内容切换的基础可用性。

#### 设备能力边界总览

- 系统级设备控制台负责设备资产、连接状态、当前绑定项目、绑定/换绑/解绑/移除、绑定记录和设备回传记录等跨项目管理能力。
- 项目级设备控制台负责读取当前项目绑定设备，完成作业规划、MQTT 指令下发、实时地图/视频展示、任务控制、遥杆控制和项目内回传观察。
- 项目勘探页“扫描数据”负责承接设备探测结果列表、结果字段与勘探字段映射、必要字段补录、准确性研判和导入勘探成果。
- 雷达适配层和扫描数据接入当前为静态原型与本地示例数据，不声明已经完成真实 MQTT 长连接、真实设备联调、真实 GIS 或正式后端入库。

#### FR-13 雷达扫描数据接入

- 系统应在项目勘探页面的“雷达扫描/扫描数据”入口中提供设备探测结果接入能力，当前主流程为查看设备返回结果列表并导入勘探成果。
- 系统应提供模拟设备适配层，用于在无真实 MQTT 设备服务或后端网关时模拟设备列表、连接、参数下发、结果队列和事件日志。
- 系统应支持展示设备列表，至少包含设备编号、在线状态、最近心跳、绑定项目或当前任务等原型信息。
- 系统应支持从模拟适配层同步设备列表，而不是只依赖页面写死的设备卡片。
- 系统应支持用户选择设备并执行“连接设备”，连接成功后应在界面中明确显示已连接设备和连接状态。
- 隐藏或历史原型中的设备连接、`initialize_scan` 初始化参数和接入日志可作为适配层联调保留，不作为当前用户主流程的唯一入口。
- 系统应支持维护并下发初始化参数，原型参数至少包括：
  - 测线编号
  - 扫描模式
  - 覆土介质相对介电常数
  - 时间窗口（纳秒）
  - 距离比例尺（米/像素）
  - 天线配置
  - 坐标系
- 系统应在原型阶段生成初始化参数下发载荷，并在界面中展示最近一次下发结果，用于后续对接真实设备协议。
- 系统应通过模拟适配层下发初始化参数，并返回适配层确认信息、队列编号或确认时间。
- 系统应提供模拟接收队列，用户可从适配层拉取示例检测结果消息并填入检测结果接收区。
- 系统应支持粘贴或加载示例 ROS2 检测结果消息，消息中应包含 `result_json` 字符串字段。
- 系统应解析 `result_json`，拆分为测线记录和候选管线成果。
- 系统应展示测线摘要，至少包括文件名、处理时间、测线长度、检测数量、时间窗口和数据格式。
- 系统应展示候选管线成果列表，至少包括管线序号、类别、置信度、预测埋深、预测外径、材质、坐标有效性和顶点坐标。
- 系统应对解析失败、GPS 全 0、识别数量不一致、未知管线类别等情况给出明确提示。
- 系统应保留原始 `result_json`，并在接入日志中记录连接、参数下发、结果解析等关键事件。
- 扫描数据弹窗应展示当前设备返回结果数据列表，包含设备回传字段、对应勘探字段、必要补录字段、导入状态和操作入口；回传时间不作为用户编辑字段，测线文件不允许编辑。
- 扫描数据弹窗应按导入状态倒序排序并分页展示，每页 10 条；仅未导入数据允许跨分页勾选，导入成功后应更新导入状态并动态刷新当前台账。
- 扫描数据弹窗应允许用户直接编辑勘探补录字段，字典字段应与新增管点、管线时的字典下拉保持一致；无值时不设置默认值。
- 补录字段中与新增管点或管线一致的必填字段，应在字段名中以红色 `*` 标识；设备返回字段表头应体现其对应的管点/管线字段含义。
- 同一测线内多条设备返回结果若 `pipe_id` 相同，应识别为同一条管线的多次探测结果，导入时应按管线聚合关系处理，避免重复生成多条无关联管线。
- 设备接入成功后，系统应自动从模拟适配层拉取接收队列中的检测数据，不要求用户再手动发起传输。
- 系统应支持用户在数据正式接入勘探成果之前进行准确性研判，研判结果至少包括“研判通过”和“需复核”。
- 系统应支持将研判后的候选管线成果以示例数据方式加入项目勘探成果，并标记来源为雷达候选成果。
- 系统应在雷达成果加入勘探后自动生成示例地图联动点和模型联动记录，不要求用户单独点击关联地图或模型。
- 后续项目数据阶段应从勘探成果中导入数据，雷达控制台当前阶段不直接写正式项目数据台账。
- 当前阶段不要求连接真实设备、真实 GIS 地图服务或真实三维模型服务，正式勘探成果入库和联动仍需后续后端接口确认。

#### FR-14 项目级设备控制台

- 系统应在项目详情页的项目级“控制台”入口中集中展示当前项目的设备接入状态。
- 控制台允许项目负责人和勘探人员执行设备对接、探测规划下发、方向调整、任务继续、任务暂停、任务结束、遥杆控制和回传同步等作业动作。
- 当当前项目未绑定设备时，首次进入控制台应展示“未绑定设备，请联系管理员进行设备绑定”的提示，不展示设备操作区。
- 当当前项目已绑定设备时，控制台应直接展示绑定设备编号、连接状态、电池状态、当前位置与角度、任务状态、任务进度和回传队列等设备状态信息。
- 控制台各状态卡、台账计数和值文本应采用紧凑字号和固定卡片高度，突出设备编号、连接状态、电量、当前位置、方向、任务进度等关键数值，避免控制台首屏被大号文本和过高卡片挤占。
- 控制台应提供“作业模式与规划”业务入口，将作业模式、初始值、方向、起点、探测范围和下发校验组织在同一连续操作区内；当前静态原型以内置默认策略和主页面高频规划字段为主，探索/遍历/复检的可视化模式切换与完整动态字段待正式作业模式接口确认后收敛。
- 控制台不得将三类作业模式强行复用为无法区分的业务策略；探索模式、遍历模式、复检模式应保留差异化参数定义和路径规划规则，当前阶段可先以内置默认值、摘要提示或隐藏配置承接。
- 作业模式与模式参数区域应采用短标签、短提示和紧凑字段展示，不在控制台内铺陈 PRD 式长段落说明。
- 探索模式应体现未知区域盲探与自动巡线要求，默认探索行间距为 3.5m，路径规则应提示弓字形覆盖和连续多帧命中触发巡线。
- 遍历模式应体现指定区域全覆盖要求，默认遍历行间距为 0.5m，默认贴边距离为 0.3m，路径规则应提示区域内缩、弓字形全覆盖、矩形端部 U 型转弯和不规则区域分段规划。
- 复检模式应支持补扫、强化扫和精细扫子模式：补扫用于漏扫区域补全，强化扫用于低置信管线补强，精细扫用于已确认管线的高密度横切扫描；强化扫与精细扫应在业务上互斥。
- 复检子模式参数应差异化展示：补扫展示补扫行间距和补扫贴边距离；强化扫展示补强线间距；精细扫展示扫描步长，横切面长度继续由探测长度/规划范围承接。
- 作业模式切换时，控制台可联动雷达采集预留参数和规划参数，例如扫描模式、探测行间距、贴边距离、命中距离和测点数量；当前阶段仅作为前端原型默认值，不声明已经完成设备端完整模式协议联调。
- 控制台不应展示置信度、置信阈值或目标置信阈值配置字段；置信度仅作为探测记录回传和勘探成果研判中的结果数据展示，不作为前端作业参数下发。
- 控制台控车主参数应保留测线编号、WGS84 初始平面坐标、设备方向和必要的探测规划参数；起始高程不参与当前 MQTT 控车，应移入“定位扩展参数（预留）”。
- 初始 X 和初始 Y 应通过实时地图定位视图点选标记自动获取，表单中仅作为只读结果展示和确认，不允许用户手动录入坐标值。
- `detection_plan.length_m` 应由系统根据后续地图选点、测线范围或默认规划规则自动计算，页面应以“探测长度（`length_m`）”只读字段展示，不要求用户在初始值表单中手动输入。
- `point_distance_m` 命中距离属于任务判定容差，应作为高级或默认策略参数管理，不作为普通初始值字段直接暴露。
- 控制台应在初始 X、初始 Y、探测长度、探测宽度、探测行间距、命中距离和规划侧字段名下方展示简短说明，帮助用户理解字段影响范围；界面文案不需要展示 MQTT 英文字段名，但需求和协议仍需保留字段映射关系。
- 控制台不应再将“几字间距”作为独立作业参数配置项展示；MQTT `detection_plan.half_arc_width_m` 应由当前作业模式的探测行间距自动映射生成，避免同一含义出现两处配置入口。
- 作业模式与规划设置应按“作业模式、车辆定位、方向放线、起点打点、范围确认、执行命令”的顺序组织：地图先展示雷达探测小车实时定位；设置设备方向后在地图上展示方位和角度；用户地图打点后以颜色标注初始点位并回填初始 X/Y；系统基于初始点位、探测长度和探测宽度绘制矩形范围框。
- 当车辆定位、设备方向、初始点位、探测长度或探测宽度任一项缺失或无效时，控制台应禁用 `detection_plan` 下发按钮并提示缺失项，不允许执行指令下发。
- 控制台操作按钮应采用紧凑尺寸；设备对接保留在控制台顶部，下发规划归入“作业模式与规划”板块，同步回传归入设备控制区内的实时指令状态流。
- 控制台任务控制区应拆分为“任务指令”和“遥杆控制”两块：继续、暂停、结束使用同一行紧凑按钮；遥杆按钮使用独立控制盘展示，并保留归零按钮对应 `manual_drive` 零速度。
- 控制台地图区域应提供“视频直播”画面，读取绑定设备或设备列表中的 `videoStreamUrl`，用于接入实时视频流；未对接或未配置视频源时展示待对接/待配置状态，不影响地图定位与探测规划操作。
- 视频直播入口应展示设备编号、视频协议和低延迟状态；当前原型仅内嵌可直接播放的 HTTP/HLS 等浏览器可播放地址，WebRTC、HTTP-FLV、RTSP 等正式播放链路由后续设备网关或视频服务适配确认。
- 控制台首屏应以实时地图为主工作区，作业模式与规划区域应在页面内直接展示测线编号、WGS84 坐标系、初始 X/Y、设备方向、探测长度、探测宽度、测点数量和规划侧等高频规划字段，并保留下发入口；低频或预留参数不应长期挤压地图，当前静态原型不保留“更多参数设置”和“模式配置”弹窗。
- 视频直播应作为地图内悬浮小窗展示，不再独立占用地图右侧栅格宽度；地图可视区域应优先保证探测范围、起点、车辆定位和路径指引的可读性。
- 控制台应提供全屏显示切换能力；进入全屏后控制台覆盖当前浏览器视口，隐藏项目页头、左侧项目菜单和其他页面干扰，保留设备状态、作业摘要、实时地图、视频直播、设备控制、实时指令状态和探测记录台账内容；退出全屏后恢复项目详情页原布局。
- 全屏模式应支持按钮退出和 `Esc` 快捷退出。
- 扫描模式、介电常数、时间窗口、距离比例尺和天线配置当前不参与 MQTT 控车指令，应作为“雷达采集参数（预留）”折叠展示，待真实雷达采集接口确认后再接入。
- 控制台初始化数据坐标系仅保留 WGS84，不再提供 CGCS2000 或其他坐标系选项。
- 控制台正式设备控制协议应以 MQTT 为基线，平台下发指令到 `/ypd/{env}/device/{deviceCode}`，平台订阅 `/ypd/{env}/platform/{deviceCode}`，QoS 为 `1`；MQTT broker、HTTP baseUrl、用户名和密码应由部署配置提供，不在需求正文中记录敏感值。
- MQTT 消息应统一使用 `messageId`、`type`、`content`、`responseTo` 外壳；当业务内容为 JSON 时，`content` 应写入 JSON 字符串，响应消息通过 `responseTo` 指向原始 `messageId`。
- 控制台下发探测规划时，应使用 `detection_plan` 指令，内容至少包含 `center_x_m`、`center_y_m`、`heading_deg`、`length_m`、`width_m`、`half_arc_width_m`、`point_distance_m`、`point_count`、`planning_side`；其中 `half_arc_width_m` 由探测行间距自动映射，不单独展示为用户输入字段。
- 控制台应支持 `start_detection` 或 `continue_task` 继续任务，支持 `pause_task` 暂停任务，支持 `finish_task` 结束任务。
- 控制台应支持 `manual_drive` 遥杆控制，内容包含 `linear_x` 和 `angular_z`；遥杆松开或归零时必须下发 `linear_x=0`、`angular_z=0`。
- 控制台应处理设备 `ack` 响应，`ack.content.code=0` 视为成功，`1-5` 应展示对应失败原因；页面侧应以滚动实时状态流展示最近指令、ACK、消息编号和 Topic，原始消息、响应关系和处理状态仍由适配层或后端保留用于追溯。
- 控制台应接收 `battery_status`、`vehicle_pose`、`task_status` 和 `gpr_detection` 上行消息，用于更新设备状态卡、实时地图定位、任务进度和探测记录回传台账。
- 控制台展示 `task_status.state` 时应按设备协议枚举映射：`0=未开始`、`1=手动控制`、`2=矩形探测中`、`3=几字型探测中`、`4=已暂停`、`5=已结束`、`6=异常`；`stateText` 可作为车端文本补充，`end_reason` 用于结束或异常原因展示。
- 控制台应展示实时地图定位视图，基于 `vehicle_pose` 或 `gpr_detection` 中的有效坐标生成定位点；`vehicle_pose` 应在地图上渲染为雷达探测小车 SVG 样式，并按 `yaw` 角度体现车身方向；当前阶段为本地原型地图，不接入真实 GIS 服务。
- 控制台应展示探测记录回传台账，至少包含回传时间、设备编号、业务类型、文件名或测线、管线类别、置信度、定位数据、有效地图点标记和状态。
- `gpr_detection.valid_map_point=false` 时，该回传结果仅进入台账展示和原始结果留痕，不参与地图定位点生成或任务命中判断。
- HTTPS 登录、STS 获取和文件 URL 提交仅作为设备结果文件上传依赖项记录，具体鉴权凭据和示例账号不得写入需求正文。
- 项目设备绑定关系应由系统级设备控制台统一维护；当前原型阶段可由示例数据或 LocalStorage 覆盖数据提供，正式环境由后端设备绑定接口承接。

#### FR-15 系统级设备控制台

- 系统应提供系统级“设备控制台”入口，用于管理员集中查看和维护所有探地雷达设备，不依赖某一个项目上下文。
- 系统级设备控制台应提供“新增设备接入”入口，管理员可登记新设备并生成平台下发 Topic 与平台订阅 Topic；该动作仅完成设备接入登记，不自动绑定项目，也不下发项目级作业指令。
- 新增设备接入时应至少填写设备编号、设备名称、设备型号、协议类型和接入备注；设备编号必须唯一，重复时不得写入设备列表。
- 新增设备接入成功后，设备默认进入“未绑定项目、待连接、未开始”状态，并可在设备列表中继续执行设备连接或绑定项目。
- 系统级新增设备接入的环境标识固定为 `prod`，页面不提供 `test`、`dev` 等其他环境选项。
- 环境标识、平台下发 Topic 和平台订阅 Topic 均为系统展示/自动生成字段，不提供输入框、下拉框或编辑入口；管理员只能通过设备编号等可编辑字段触发 Topic 展示内容刷新。
- MQTT 设备接入的 Topic 模板应按 `/ypd/prod/device/{deviceCode}` 和 `/ypd/prod/platform/{deviceCode}` 自动生成；Broker、用户名、密码、Token 等凭据只来自部署配置，不在页面表单录入。
- 系统级设备控制台应支持移除设备接入登记。仅未绑定项目的设备允许移除；已绑定项目的设备应禁用移除入口，并提示先解绑项目后再移除设备。
- 移除设备前应进行二次确认并要求填写或确认移除原因；移除成功后设备不再出现在全部设备列表中，但历史绑定流水、设备回传记录和审计记录不应被物理删除。
- 系统级设备控制台应提供“设备连接”按钮，管理员可对单台设备发起连接、重新连接或刷新连接状态；连接结果应展示成功、失败、超时、离线等状态，并记录最近连接时间和连接来源。
- 系统级设备控制台中的“设备连接”只负责建立或校验设备与平台/网关的连接状态，不应直接下发项目级 `detection_plan`、继续、暂停、结束或遥杆控制等作业指令。
- 系统级设备控制台应展示全部设备列表，至少包含设备编号、设备名称或型号、协议类型、在线状态、连接状态、最近心跳、最近回传时间、当前任务状态、当前绑定项目、绑定状态和操作入口。
- 设备列表应支持按设备编号/名称、在线状态、连接状态、绑定状态、当前绑定项目进行查询或筛选。
- 设备详情应展示设备基础信息、当前状态、当前绑定项目、最近连接记录、最近回传摘要、绑定记录和设备回传记录。
- 当前绑定项目应展示项目编号、项目名称、项目状态、绑定时间、绑定人和绑定备注；无绑定时应明确展示“未绑定项目”。
- 绑定记录应展示设备历史绑定、换绑和解绑流水，至少包含设备编号、操作类型、原项目、新项目、操作人、操作时间、操作原因或备注、处理状态。
- 设备回传记录应展示设备上行消息或结果文件回传台账，至少包含回传时间、设备编号、消息类型、关联项目、消息编号、响应指令、返回结果、处理状态、摘要信息和详情入口；协议层仍需保留 `messageId`、`responseTo` 和 Topic 便于追溯。
- 系统级设备列表和设备详情中的当前任务状态应复用 MQTT `task_status.state` 的 `0-6` 枚举中文展示口径。
- 设备回传记录中的 `battery_status`、`vehicle_pose`、`task_status`、`gpr_detection` 等消息应按类型分类展示；原始消息内容可在详情中查看，但敏感鉴权凭据、HTTPS token、STS 临时凭据不得在页面明文展示。
- 系统级设备控制台应支持将未绑定设备绑定到项目。绑定时管理员需选择目标项目，系统展示目标项目当前绑定状态，并在确认后写入新的设备项目绑定关系。
- 系统级设备控制台应支持将已绑定设备换绑到其他项目。换绑时系统应展示原项目和新项目，要求管理员确认换绑原因，并在成功后关闭原绑定、创建新绑定、同步更新设备和项目侧的当前绑定状态。
- 当设备处于离线状态（`online=false` 或连接状态为 `offline`）时，系统级设备控制台应禁用该设备的换绑入口；即使从弹窗或接口层触发换绑，也应拦截并提示“离线设备不可换绑，请先恢复连接”。
- 系统级设备控制台应支持解绑设备。解绑时系统应展示当前绑定项目，要求管理员确认解绑原因；解绑成功后该设备变为未绑定状态，原项目进入“未绑定设备，请联系管理员进行设备绑定”的项目级控制台分流。
- 绑定、换绑、解绑操作应有二次确认，且应在失败时展示失败原因；操作成功后设备列表、设备详情、项目级控制台绑定状态和绑定记录应同步刷新。
- 当设备处于作业中、任务未结束、连接异常或存在未处理回传记录时，系统应根据后端规则限制换绑或解绑；当前原型阶段应至少展示风险提示并保留待确认状态。
- 系统级设备控制台应区分设备管理操作与项目作业操作：管理员维护设备和项目关系，项目负责人/勘探人员在项目级控制台内执行作业规划和控制命令。

## 5. 业务规则

- BR-01 项目编号应采用类似 `PROJ-YYYY-NNN` 的结构；具体生成规则后续由后端或统一编码服务确认。
- BR-02 业主编号应采用类似 `OWN-YYYY-NNN` 的结构。
- BR-03 项目场景类型当前至少包括：地下管线普查、地下管线详查、地下管线放线测量、地下管线竣工测量。
- BR-04 项目状态当前至少包括：未开始、实施中、已完成、超期、停工。
- BR-05 业主状态当前至少包括：启用、未启用。
- BR-06 项目详情、勘探、数据、模型、报告页面之间切换时，应保持对同一项目上下文的识别能力。
- BR-07 项目数据导入时，系统应区分更新数据与新增数据。
- BR-08 数据导入表格中高亮字段应视为需要重点校核的差异项或风险项。
- BR-09 知识中心虽然尚未完成，但入口不应消失，以体现后续建设规划。
- BR-10 当前代码仓库中大量业务数据为示例数据或本地存储数据，正式环境需由后端接口替代。
- BR-11 雷达设备接入原型中，未连接设备前不得下发初始化参数。
- BR-12 雷达设备接入原型中的模拟初始化参数下发应生成结构化载荷，命令名可保持为 `initialize_scan`；项目级控制台的正式设备控制应以 MQTT `detection_plan` 等协议指令为准。
- BR-13 雷达检测结果中的 `result_json` 必须先通过 JSON 解析与结构校验，解析失败时不得进入候选成果列表。
- BR-14 `metadata.total_pipes_detected` 应与 `pipes.length` 做一致性校验，不一致时应展示警告但保留可查看结果。
- BR-15 管线类别映射规则当前为：`PW=给水`、`PG=燃气`、`EC=电力`、`MW=给水`，未知类别进入“其他/待确认”。
- BR-16 管线材质映射规则当前为：`0=非金属`、`1=金属`，未知值展示为“待确认”。
- BR-17 GPS 坐标全为 `0.0` 时应标记为“无有效坐标”，不得默认作为正式成果坐标。
- BR-18 雷达候选成果在第一阶段只进入原型预览和接入日志，不自动覆盖或新增项目数据台账记录。
- BR-19 第二阶段适配层接入采用示例数据实现，不得声称已完成真实设备联调。
- BR-20 页面侧设备连接、初始化参数下发和结果接收应通过适配层方法完成，适配层返回结构应接近后续真实接口形态。
- BR-21 适配层下发初始化参数时，应记录确认编号、设备编号、下发载荷和时间，便于后续替换为真实接口回执。
- BR-22 适配层结果队列中的消息应保留原始 ROS2 DetectionResult 形态，页面解析前不得提前丢弃 `result_json` 原文。
- BR-23 设备接入成功后应自动拉取适配层接收队列中的检测消息；手动拉取仅作为刷新或补偿动作。
- BR-24 第三阶段成果接入仍使用 LocalStorage 示例数据，写入目标为项目勘探成果本地存储结构。
- BR-25 雷达候选成果接入勘探时应生成稳定的勘探成果编号，编号前缀应与平台管线分类规则一致，例如给水使用 `EXP-W`、燃气使用 `EXP-G`、电力使用 `EXP-E`。
- BR-26 雷达勘探成果应保留来源测线、置信度、设备来源、准确性研判结果、原始类别代码等追溯信息。
- BR-27 已存在同一雷达候选成果 ID 时不应重复接入勘探，应返回跳过记录。
- BR-28 第四阶段地图/模型联动使用勘探成果自动派生的示例联动数据，不得声明已完成真实地图或真实三维模型接入。
- BR-29 地图/模型联动记录应与勘探成果保持引用关系，包含候选成果 ID、坐标、类别、埋深、外径和测线文件名。
- BR-30 项目级控制台进入时必须先判断当前项目是否存在设备绑定；无绑定时只显示联系管理员绑定设备的提示。
- BR-31 项目级控制台中的设备对接、探测规划下发、任务控制和回传同步均应复用设备适配层、MQTT 网关或等价接口，不应在页面内伪造互不关联的状态。
- BR-32 项目级控制台下发的 `detection_plan` 载荷应包含探测起点、方向、矩形区域和几字型测点参数，字段应可追溯到当前项目和绑定设备。
- BR-33 项目级控制台的回传台账来源为设备检测结果解析后的本地原型数据，不等同于正式项目数据台账入库。
- BR-34 项目级控制台的用户侧初始化坐标系固定为 WGS84，界面不得再保留其他坐标系选择；MQTT `detection_plan` 所需米制坐标由适配层或后端转换承接，转换算法和责任边界需后续确认。
  - 作业模式选择应先于探测规划参数展示；探索、遍历、复检模式应分别带出对应默认行间距、贴边距离和路径说明，复检模式下的补扫、强化扫、精细扫子模式应保留当前选择状态。
  - 当前 MQTT `detection_plan` 仍以协议字段为下发基线；作业模式、复检子模式、行间距和贴边距离属于前端作业策略参数，后续是否扩展进正式 MQTT 业务内容需与设备端和后端确认。
- BR-35 MQTT 下发命令必须记录 `messageId`、`type`、目标 Topic、环境标识、设备编号、`content`、发送时间、响应状态和响应时间，便于指令追踪和问题排查。
- BR-36 MQTT JSON 业务内容必须序列化为 `content` 字符串，不得直接下发嵌套对象导致车端解析不一致。
- BR-37 设备 `ack` 只有在 `responseTo` 匹配原始 `messageId` 时才能更新该指令状态；`code=0` 标记成功，`code=1-5` 或超时标记失败并展示原因。
- BR-38 遥杆控制应遵守车端限幅约束，平台侧可提前限制 `linear_x` 和 `angular_z` 的输入范围；遥杆归零必须发送零速度指令。
- BR-39 `battery_status`、`vehicle_pose`、`task_status` 应分别映射到电量状态、地图定位和任务进度展示，不得与探测结果台账混淆。
- BR-40 `gpr_detection.valid_map_point=false` 的探测结果不得参与地图定位、矩形命中或几字型测点判断，但应保留原始结果用于追溯。
- BR-41 MQTT broker、HTTP baseUrl、用户名、密码和 HTTPS token 应作为部署配置或后端网关配置管理，需求文档不得记录账号口令明文。
- BR-42 设备结果文件上传依赖 HTTPS 登录、STS Token 和文件 URL 提交流程时，应仅记录接口能力和鉴权依赖，不在前端需求中固化示例凭据。
- BR-43 系统级设备控制台是设备项目绑定关系的管理入口；项目级控制台只读取当前项目绑定结果并执行作业控制，不直接维护跨项目绑定关系。
- BR-44 同一设备同一时刻只能存在一个有效绑定项目；同一项目当前默认只允许绑定一台主控雷达设备，后续如需多设备并行应扩展设备角色和绑定类型规则。
- BR-45 绑定设备到项目时，应校验设备存在、设备未被其他项目有效绑定、目标项目存在且具备接入设备的业务状态；校验失败时不得写入绑定关系。
- BR-46 换绑设备时，应先关闭原项目绑定记录，再创建新项目绑定记录，并保留原项目、新项目、操作人、操作时间、操作原因和处理状态。
- BR-47 解绑设备时，应关闭当前有效绑定记录，并同步清空设备的当前绑定项目；解绑后的原项目进入项目级控制台未绑定提示分流。
- BR-48 设备离线时不得换绑，需先恢复连接后再执行换绑；设备处于作业中、任务未结束、其他连接状态异常或存在待处理回传记录时，换绑和解绑应被限制或要求管理员确认风险，正式限制条件由后端设备任务状态规则确认。
- BR-49 系统级设备连接按钮只用于连接、重连或刷新设备与平台/网关的连接状态，不应绕过项目级控制台下发任何作业指令。
- BR-50 系统级设备回传记录应按设备、项目、消息类型、`messageId`、`responseTo`、Topic 和处理状态可追溯；涉及凭据、Token 或临时访问密钥的内容不得明文展示。
- BR-51 系统级新增设备接入应先完成设备编号唯一性校验；接入成功只创建设备登记记录和协议路由信息，不创建项目绑定关系。
- BR-52 系统级移除设备只允许作用于未绑定设备；已绑定设备必须先解绑项目，移除设备登记不得物理删除历史绑定流水、设备回传记录和审计记录。

设备绑定操作规则摘要：

| 操作 | 前置条件 | 硬阻断 | 成功结果 | 留痕要求 |
| --- | --- | --- | --- | --- |
| 绑定项目 | 设备存在且未被其他项目有效绑定；目标项目存在 | 设备已绑定其他项目、设备不存在、目标项目不存在 | 写入新的有效绑定关系 | 记录设备、目标项目、操作人、时间和备注 |
| 换绑项目 | 设备已绑定项目且在线；目标项目存在 | 设备离线、目标项目与原项目相同、目标项目不存在 | 关闭原绑定并创建新绑定 | 记录原项目、新项目、操作原因和处理状态 |
| 解绑项目 | 设备存在有效绑定关系 | 设备不存在或无有效绑定 | 关闭当前绑定，项目级控制台回到未绑定提示 | 记录原项目、解绑原因、操作人和时间 |
| 移除设备 | 设备未绑定项目 | 已绑定项目 | 从全部设备列表移除接入登记 | 不物理删除历史绑定流水、设备回传记录和审计记录 |

## 6. 非功能需求

- 全文界面默认使用中文。
- 系统应兼容桌面端常见分辨率，并考虑平板/窄屏场景下的导航适配。
- 页面应具备基础响应式能力，导航在窄屏下可横向滚动或简化展示。
- 系统应支持深色模式。
- 系统应保证主要操作反馈清晰，例如按钮状态、弹窗显隐、编辑结果可见。
- 系统前端资源组织应保持模块化，导航、项目参数传递、工具函数与模块脚本应可复用。
- 现阶段系统为前端原型，性能目标以页面可流畅展示、交互无明显阻塞为主。
- 雷达结果解析逻辑应抽离为可测试的前端模块，便于后续替换为真实后端接口或设备适配层。
- 雷达适配层逻辑应抽离为可测试的前端模块，接口命名应尽量贴近后续真实设备服务。
- 雷达成果接入勘探与地图/模型自动联动转换逻辑应抽离为可测试模块，避免散落在页面事件中。
- 雷达接入原型应保留原始消息与解析日志，便于后续联调排查。
- 项目级控制台的 MQTT 指令和上行消息应保留可追溯台账，便于按设备、项目、消息类型和 `messageId` 排查联调问题。

## 7. 风险与依赖

- 当前多数页面以静态 HTML 和前端脚本为主，真实后端尚未接入，存在需求与实现脱节风险。
- 当前使用示例数据和 LocalStorage 模拟数据，正式数据模型尚未冻结。
- 登录、权限、地图、数据导入、模型、报告等能力存在“原型已展示但真实链路未完成”的情况。
- 知识中心处于建设中，内容结构已出现但资料体系尚未落地。
- 项目勘探、模型、报告等模块在导航层面已明确，但详细业务规则仍需补充。
- 系统级设备控制台与项目级控制台当前仍存在本地示例数据和 LocalStorage 覆盖数据并存的情况，正式跨页绑定状态同步需由统一后端设备绑定接口承接。
- 雷达小车与平台的设备控制协议已以 MQTT 对接协议作为需求基线，但真实联调、后端网关、部署鉴权、断线重连和消息重试策略仍需确认。
- 控制台用户侧仅保留 WGS84 坐标，MQTT `detection_plan` 所需米制坐标的转换算法、投影参数和责任边界仍需确认。
- `result_json` 中 CGCS2000 坐标示例仍表现为经纬度格式，正式接入前需确认与 WGS84、米制坐标和勘探地图坐标之间的转换关系。
- 雷达候选成果与项目勘探成果之间的编号生成、重复判断和准确性研判规则尚未冻结。
- 地图服务和三维模型服务尚未接入，当前仅展示基于候选成果坐标生成的示例联动数据。

## 8. 验收标准

- 雷达扫描数据接入应能完成以下验收：
  - 选择设备后可显示连接成功状态。
  - 未连接设备时，下发初始化参数应被阻止并提示。
  - 连接设备后可生成并展示初始化参数下发载荷。
  - 页面可从模拟适配层同步设备列表并展示适配层在线状态。
  - 页面可通过模拟适配层拉取接收队列中的示例检测消息。
  - 初始化参数下发后应显示适配层回执编号或确认信息。
  - 粘贴或加载示例检测结果后，系统可解析 `result_json` 并展示测线摘要。
  - 系统可展示每条候选管线的类别、置信度、埋深、管径、材质和顶点坐标。
  - 非法 JSON 应显示解析失败提示，不得导致页面脚本报错。
  - 接入日志应记录连接、参数下发、结果解析成功或失败事件。
  - 设备连接成功后，系统应自动传输并载入一条示例检测结果。
  - 用户可在候选成果接入前选择准确性研判结果。
  - 用户确认后，候选成果应写入本地勘探成果存储，并看到新增、跳过重复的数量反馈。
  - 勘探成果记录应包含来源为雷达候选成果的追溯字段和准确性研判结果。
  - 用户可查看由勘探成果自动生成的示例地图点和模型定位记录。
  - 地图/模型联动展示应明确为勘探成果自动派生的示例联动，不依赖真实 GIS 或三维服务。
  - 扫描数据弹窗应展示设备返回结果、对应勘探字段、必填补录字段、导入状态和分页；仅未导入数据可跨分页勾选并批量导入。
  - 同一测线内 `pipe_id` 相同的多条返回结果应在导入时按同一管线关系处理，避免重复生成无关联管线。
- 项目级设备控制台应能完成以下验收：
  - 未绑定设备进入控制台时，只显示“未绑定设备，请联系管理员进行设备绑定”的提示，不展示设备操作区。
  - 已绑定设备进入控制台时，可查看绑定设备编号、连接状态、电池状态、当前位置与角度、任务状态、实时地图定位和探测记录回传台账。
  - 控制台应展示“作业模式与规划”区，主页面常驻展示起点、方向、探测长度、探测宽度、测点数量和规划侧等高频规划字段；作业模式可先以内置默认策略和摘要提示承接。
  - 探索、遍历、复检及补扫、强化扫、精细扫的完整可视化切换和动态参数字段，应在正式作业模式接口确认后再补齐验收。
  - 作业参数配置中不应出现置信度阈值或目标置信阈值输入字段；探测结果中的置信度仍应作为回传结果展示。
  - 用户在实时地图定位视图中点击或标记探测起点后，初始 X 和初始 Y 自动回填到只读字段；不得要求用户直接手动输入初始坐标。
  - 下发 `detection_plan` 时，消息外壳应包含 `messageId`、`type`、`content`、`responseTo`，且 `content` 为 JSON 字符串。
  - 只有 `responseTo` 匹配原始 `messageId` 的 `ack` 才能更新该指令结果；`ack.code=0` 显示成功，非 `0` 或超时显示失败原因，并在设备控制区的实时指令状态流中滚动展示。
  - 控制台可发起继续、暂停、结束和遥杆控制指令，遥杆松开或归零时必须发送 `manual_drive` 零速度。
  - 收到 `battery_status`、`vehicle_pose`、`task_status`、`gpr_detection` 后，应分别更新状态卡、地图定位、任务进度和回传台账；其中 `vehicle_pose` 应在地图中展示雷达探测小车 SVG 实时位置和方向。
  - 地图区域应同时显示设备视频直播面板；设备已对接且存在 `videoStreamUrl` 时展示视频源状态、协议和直播入口，未对接或未配置时展示对应待处理状态。
  - 控制台首屏应优先展示紧凑设备状态、主页面起点/方向/范围规划字段、大尺寸地图、地图内视频悬浮窗和设备控制；低频或预留参数不应长期占据左侧页面高度。
  - 用户点击“全屏显示”后，控制台应覆盖浏览器视口并扩大可用工作区；点击“退出全屏”或按 `Esc` 后应恢复普通项目详情布局。
  - `gpr_detection.valid_map_point=false` 时，不生成地图定位点，也不参与任务命中判断。

- 系统级设备控制台应能完成以下验收：
  - 管理员进入系统级设备控制台后，可以看到全部设备列表，并查看设备编号、在线状态、连接状态、最近心跳、最近回传时间、当前绑定项目和绑定状态。
  - 管理员点击“新增设备接入”后，应能填写设备编号、名称、型号和协议，并看到不可编辑的固定环境标识 `prod`、平台下发 Topic 与平台订阅 Topic 自动生成。
  - 新增设备编号重复时，系统应提示设备编号已存在；新增成功后设备出现在全部设备列表中，状态为未绑定、待连接、未开始。
  - 对未绑定设备执行移除操作时，系统应二次确认，成功后设备从全部设备列表移除。
  - 对已绑定设备执行移除操作时，移除入口应禁用或提示先解绑项目后再移除设备。
  - 管理员点击单台设备的“设备连接”按钮后，系统应展示连接中、连接成功、连接失败或超时状态，并记录最近连接时间；该动作不得下发项目级作业指令。
  - 管理员打开设备详情后，可以查看设备基础信息、当前状态、当前绑定项目、绑定记录和设备回传记录。
  - 设备绑定记录应能区分绑定、换绑、解绑操作，并展示原项目、新项目、操作人、操作时间、原因或备注和处理状态。
  - 设备回传记录应按消息类型展示 `battery_status`、`vehicle_pose`、`task_status`、`gpr_detection` 等上行结果，并使用中文列名展示消息编号、响应指令、返回结果、关联项目、处理状态和摘要；协议字段仍应可追溯。
  - 对未绑定设备执行绑定项目操作时，系统应要求选择目标项目，成功后设备列表、设备详情和目标项目的项目级控制台均显示新的绑定关系。
  - 对已绑定设备执行换绑项目操作时，系统应展示原项目和新项目，要求填写或确认换绑原因；成功后原项目不再显示该设备，新项目显示该设备，绑定记录新增换绑流水。
  - 对已绑定设备执行解绑操作时，系统应二次确认；成功后设备显示为未绑定，原项目的项目级控制台进入“未绑定设备，请联系管理员进行设备绑定”的提示状态。
  - 当设备离线时，已绑定设备的换绑入口应禁用，触发换绑时提示“离线设备不可换绑，请先恢复连接”。
  - 当设备处于作业中或存在未处理回传记录时，换绑/解绑操作应展示限制或风险提示，并保留失败或待确认状态。

## 9. 本次迭代更新摘要

- 迭代八补丁（v0.8.6）：整理设备控制与扫描数据需求边界。
  - 明确系统级设备控制台、项目级设备控制台、项目勘探扫描数据接入三者的职责边界，避免设备资产管理、项目作业控制和扫描结果导入混写。
  - 将系统级控制台口径统一为一级“控制台”入口，项目工作台补充项目级“控制台”页签。
  - 将 FR-13 从“雷达设备接入原型”收敛为“雷达扫描数据接入”，补充扫描数据弹窗、字段映射、必填补录、分页勾选、导入状态和同 `pipe_id` 聚合规则。
  - 修正项目级控制台当前原型口径：移除“当前方向”状态卡描述，不再声明保留“更多参数设置”和“模式配置”弹窗；探索/遍历/复检完整可视化模式切换仍作为待确认能力。
  - 将系统级回传台账展示字段改为中文口径，同时保留 `messageId`、`responseTo`、Topic 等协议字段追溯要求。
  - 历史版本摘要不再堆叠在 PRD 正文中，完整版本链以 `spec-log.md` 为准。

## 10. 待确认事项

- 登录账号体系是否继续使用手机号登录，还是需要支持账号/工号/单点登录。
- 项目、业主、用户、组织、角色的正式数据模型与接口协议尚未确认。
- 项目勘探页面中的完整作业流程、真实联调排障流程和现场问题闭环规则尚未确认。
- MQTT 网关真实部署形态、后端代理边界、鉴权配置、心跳保活、断线重连、消息重试和超时策略尚未确认。
- 探索、遍历、复检作业模式参数是否需要写入正式 MQTT `detection_plan.content`，以及设备端是否需要新增独立作业模式指令，仍需与设备端和后端确认。
- WGS84 初始化坐标转换为 MQTT `detection_plan` 米制坐标的算法、投影参数、转换责任边界和误差处理规则尚未确认。
- 雷达初始化与探测规划字段是否需要扩展设备姿态、采样率、天线间距、任务编号等信息尚未确认。
- 雷达候选成果进入勘探成果时的编号生成、坐标转换、重复冲突和准确性研判细则尚未确认。
- 勘探成果进入正式项目数据台账时的导入、审核流、权限、冲突处理和回滚规则尚未确认。
- 勘探成果自动派生的地图/模型联动后续是否进入独立 GIS 服务、模型服务或项目工作台统一状态管理尚未确认。
- 项目数据页中的导入模板、字段校验规则、差异处理方式和最终入库策略尚未确认。
- 项目模型与项目报告页面的详细功能边界、输入输出和验收标准尚未确认。
- 知识中心的内容来源、发布机制、分类体系和权限控制尚未确认。
- 系统级设备控制台的正式后端接口尚未确认，包括设备列表、设备连接、绑定项目、换绑项目、解绑、绑定记录查询和设备回传记录查询。
- 设备与项目是否支持“一项目多设备”“一设备多任务”以及设备主控/备用/只读等绑定类型尚未确认，当前需求按单设备单项目有效绑定记录描述。
- 系统级设备控制台的操作权限、审批要求、作业中强制换绑/解绑限制条件和异常回滚规则尚未确认。
